// https://leetcode.com/problems/generate-parentheses/description/



function generateParenthesis(n: number): string[] {
  const res:string[] = []
  function createString (current?:string, open?:number, close?:number){
    if(current?.length ===   )
    }
  let left = "(";
  let right = "(";
  let root = [left];
  while 
}

console.log(generateParenthesis(3));
